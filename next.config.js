/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output:'export',
    basePath : "/kh595.github.io",
    trailingSlash: true,
}

module.exports = nextConfig
